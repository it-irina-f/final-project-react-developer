import styled from "@emotion/styled";

export const TickLabelWrapper = styled.div`
  display: block;
  margin-left: ${(props) => props.offset}px;
  width: ${(props) => props.widthBar}px;
  color: #574c4c;
  text-align: center;
  font-size: 14px;
  font-weight: bold;
`;


