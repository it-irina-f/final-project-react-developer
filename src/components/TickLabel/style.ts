import styled from "styled-components";

export const TickLabelWrapper = styled.span`
  display: block;
  margin-left: 20px;
  width: ${(props) => props.widthBar}px;
  color: #574c4c;
  text-align: center;
  font-size: 14px;
  font-weight: bold;
`;
